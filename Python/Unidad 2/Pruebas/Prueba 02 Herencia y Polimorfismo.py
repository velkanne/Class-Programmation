# Unidad 2: Nivel Intermedio
# Prueba 2: Herencia y Polimorfismo

# 1. Crea una clase base `Figura` con un método `area` que simplemente pase (pass).
# 2. Crea dos clases que hereden de `Figura`: `Rectangulo` y `Circulo`.
# 3. El constructor de `Rectangulo` debe aceptar `base` y `altura`.
# 4. El constructor de `Circulo` debe aceptar `radio`.
# 5. Sobrescribe el método `area` en ambas clases para que calcule y devuelva el área correcta.
#    (Área del rectángulo: base * altura, Área del círculo: pi * radio^2).
#    Usa `math.pi` para el valor de pi.
# 6. Crea una instancia de `Rectangulo` y otra de `Circulo` e imprime sus áreas.

# Escribe tu código aquí (no olvides importar math)
