# Unidad 3: Nivel Avanzado
# Prueba 1: Decoradores y Generadores

# 1. Crea un decorador `medir_tiempo` que mida cuánto tiempo tarda en ejecutarse una función.
#    Debe imprimir "La función [nombre] tardó X.XXXX segundos."
#    Pista: usa el módulo `time` y la función `time.time()`.

# 2. Crea una función `proceso_largo()` que simplemente espere 2 segundos (`time.sleep(2)`).
#    Aplícale el decorador `medir_tiempo`.

# 3. Crea una función generadora `numeros_pares(limite)` que genere números pares
#    desde 0 hasta el `limite` (incluido).

# 4. Llama a `proceso_largo()` y luego usa un bucle `for` para imprimir los números
#    generados por `numeros_pares(10)`.

# Escribe tu código aquí
